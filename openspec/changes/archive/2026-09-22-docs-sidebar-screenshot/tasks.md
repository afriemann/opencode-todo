# Tasks

## 1. Add screenshot to README

- [x] 1.1 Copy `~/screenshot.png` into the repo at `docs/sidebar-screenshot.png` and verify the
      file is a valid PNG committed to the repo.
- [x] 1.2 Add an image embed in `README.md`'s "## TUI Sidebar" section referencing
      `docs/sidebar-screenshot.png`, and verify the markdown renders correctly (visual check).

## 2. Quality gates

- [x] 2.1 Run the full test suite, typecheck, and linter and confirm no failures (docs-only change
      should have zero effect, but confirm nothing broke).
- [ ] 2.2 Get the change reviewed by `code-reviewer` and resolve every `[BLOCKER]`/`[WARNING]`.
